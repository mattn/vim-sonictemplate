apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{_cursor_}}
  labels:
    app: 
spec:
  replicas: 1
  template:
    metadata:
      name: 
      labels:
        app: 
    spec:
      containers:
        - name: 
          image: 
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
  selector:
    matchLabels:
      app: 

